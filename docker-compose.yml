services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: job_scheduler
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "0000"
    ports:
      - "5433:5432"
    volumes:
      - job_scheduler_pgdata:/var/lib/postgresql/data

volumes:
  job_scheduler_pgdata: