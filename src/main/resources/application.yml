spring:
  application:
    name: job-scheduler

  datasource:
    url: jdbc:postgresql://localhost:5432/job_scheduler
    username: postgres
    password: ${DB_PASSWORD:0000}

  jpa:
    hibernate:
      ddl-auto: none   # schema is Flyway-owned

  flyway:
    enabled: true
    locations: classpath:db/migration